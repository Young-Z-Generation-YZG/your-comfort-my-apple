const svgs = {
   appleIcon: require('./apple-icon.svg').default,
   appleSearchIcon: require('./apple-search-header.svg').default,
   appleBasketIcon: require('./apple-basket.svg').default,
};

export default svgs;
