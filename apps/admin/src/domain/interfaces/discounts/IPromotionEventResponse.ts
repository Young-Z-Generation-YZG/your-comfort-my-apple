export interface IPromotionEventResponse {
   promotion_event_id: string;
   promotion_event_title: string;
   promotion_event_description: string;
   promotion_event_type: string;
   promotion_event_state: string;
   promotion_event_valid_from: string;
   promotion_event_valid_to: string;
}
